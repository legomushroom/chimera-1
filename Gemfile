# frozen_string_literal: true

ruby "3.0.0"

source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in chimera.gemspec.
gemspec

group :development, :test do
  gem "debase"
  gem "dotenv-rails"
  gem "puma"
  gem "rspec-rails"
  gem "rubocop"
  gem "rubocop-rspec"
  gem "ruby-debug-ide"
  gem "solargraph"
end
